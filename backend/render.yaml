services:
  - type: web
    name: htc2026-backend
    runtime: python
    buildCommand: pip install -r requirements.txt && pip install -r pop_pipeline/requirements.txt
    startCommand: cd pop_pipeline && uvicorn api.main:app --host 0.0.0.0 --port $PORT